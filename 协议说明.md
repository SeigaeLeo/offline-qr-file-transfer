# QTX1 动态二维码协议

## 帧格式

二维码内容全部是 QR 字母数字字符。固定头部为 35 个字符：

| 偏移 | 长度 | 含义 |
|---:|---:|---|
| 0 | 4 | 魔数 `QTX1` |
| 4 | 1 | 类型：`S` 元数据、`D` 原始数据、`R` 变形补发数据、`E` 本轮结束 |
| 5 | 10 | 会话 ID，5 个随机字节的大写十六进制 |
| 15 | 6 | 分片序号，Base36，大写、左侧补零 |
| 21 | 6 | 数据分片总数，Base36，大写、左侧补零 |
| 27 | 8 | 解码后载荷的 CRC32，大写十六进制 |
| 35 | 可变 | Base45 载荷 |

CRC32 使用标准 IEEE 多项式，测试字符串 `123456789` 的结果必须是 `CBF43926`。

## Base45

字符表：

```text
0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ $%*+-./:
```

两个输入字节组合为 0～65535 的整数，编码为三个 Base45 字符；末尾单字节编码为两个字符。算法与 RFC 9285 的 Base45 数据变换一致。

## S 帧

载荷是 UTF-8 JSON，经 Base45 编码。当前字段：

- `v`：协议版本，当前为 1。
- `n`：文件名。
- `m`：MIME 类型。
- `s`：原始字节数。
- `c`：每个数据分片的最大字节数。
- `t`：数据分片总数。
- `h`：原始字节的 SHA-256，大写十六进制。
- `z`：压缩算法，当前为 `NONE`。
- `e`：加密算法，当前为 `NONE`。

## D 帧

序号从 0 开始。载荷是对应文件片段的原始字节，经 Base45 编码。手机先验证 CRC，再按序号写入本地临时数据库。重复帧不重复计数。

## R 帧

用于偶数补发轮次。载荷首字节是 1～255 的图案盐值，后续字节与同序号 D 帧的原始文件片段相同。CRC 覆盖盐值和文件片段；手机校验 CRC 后移除盐值再保存。盐值让同一困难分片在不同补发轮次生成不同的二维码图案。

首轮和奇数补发轮次仍使用 D 帧，因此旧版接收端虽然不识别 R 帧，仍能隔轮继续补片。

## E 帧

载荷为空，表示发送端已经播放完一轮。它不是接收成功证明；手机若有缺片，应继续接收下一轮。

## 补发顺序

首轮按 0 到 `t - 1` 顺序发送。后续轮次把整个文件最多分成 16 条带，并在条带之间交错发送，例如 500 帧近似为 `0, 32, 64, ... , 480, 1, 33, 65, ...`。每轮每个数据帧仍只发送一次；轮次之间会旋转起始条带，降低连续漏帧集中在同一区域的概率。

## 完成条件

只有同时满足以下条件才算成功：

1. 0 到 `t - 1` 的分片全部存在。
2. 拼接长度等于 `s`。
3. SHA-256 等于 `h`。
